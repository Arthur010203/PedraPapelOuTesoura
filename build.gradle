// Nível máximo onde as construções das configurações podem ser alteradas tanto nos sub projetos quanto nos modulares.

buildscript {
    repositories {
        google()
        jcenter()
        
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.5.2'
        
        // NOTA: Não coloque as dependências do seu aplicativo aqui; elas pertencem
        // aos arquivos build.gradle do módulo individual
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
